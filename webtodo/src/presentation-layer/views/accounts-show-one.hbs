<div class="content">
<h1>Account</h1>
{{#if errors.length}}
	<p>Oh no, something went wrong:</p>
	<ul>
		{{#each errors}}
			<li>{{this}}</li>
		{{/each}}
	</ul>
{{else}}
	
	{{#if accounts}}
		<dl>
			
			<dt>Id:</dt>
			<dd>{{accounts.id}}</dd>
			
			<dt>Username:</dt>
			<dd>{{accounts.username}}</dd>
			
		</dl>
	{{else}}
		<p>No, that account doesn't exist.</p>
	{{/if}}
	
{{/if}}
</div>